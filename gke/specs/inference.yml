apiVersion: apps/v1
kind: Deployment
metadata:
  name: $MODEL_NAME-deployment
  labels:
    app: $MODEL_NAME
spec:
  replicas: 1
  selector:
    matchLabels:
      app: $MODEL_NAME
  template:
    metadata:
      labels:
        app: $MODEL_NAME
    spec:
      serviceAccountName: $KSA_NAME
      nodeSelector:
        cloud.google.com/gke-accelerator: "$ACCELERATOR_TYPE"
      containers:
      - name: triton
        image: "$INFERENCING_IMAGE_URI"
        imagePullPolicy: Always
        env:
        - name: AIP_STORAGE_URI
          value: "$MODEL_UPLOAD_PATH"
        ports:
        - containerPort: 5000
        resources:
          limits:
            cpu: 4
            memory: 16Gi
            nvidia.com/gpu: "$GPU_NUMBER"
          requests:
            cpu: 50m
            memory: 50Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 3
          failureThreshold: 10

---
apiVersion: v1
kind: Service
metadata:
  name: $MODEL_NAME
spec:
  selector:
    app: $MODEL_NAME
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: ClusterIP